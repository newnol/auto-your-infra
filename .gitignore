# Ansible Vault
ansible/vault_pass.txt
# Note: ansible/group_vars/all/secrets.yml is intentionally NOT ignored
# as it contains encrypted Ansible Vault data which is safe to commit

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
terraform.tfvars

# OS
.DS_Store
Thumbs.db

# Logs
*.log
