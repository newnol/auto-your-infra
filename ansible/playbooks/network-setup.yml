---
- hosts: localhost
  vars_files:
    - ../group_vars/all/secrets.yml
  tasks:
    - name: Validate Proxmox API credentials are loaded
      debug:
        msg: "Proxmox API user is configured: {{ proxmox_api_user is defined }}"

    - name: Example firewall config (placeholder)
      debug:
        msg: "Configuring firewall for port {{ nginx_port | default(8080) }}"
      # In real setup, use firewalld module
      # firewalld:
      #   port: "{{ nginx_port | default(8080) }}/tcp"
      #   permanent: yes
      #   state: enabled
      # become: yes